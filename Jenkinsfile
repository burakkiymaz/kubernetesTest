pipeline {
    agent any

    stages("build") {
        steps {
            echo "Building the application"
        }
    }
    stages("test") {
        steps {
            echo "Testing the application"
        }
    }
    stages("deploy") {
        steps {
            echo "Deploy the application"
        }
    }
}